{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tesoreronacional</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tesoreroNacional.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tesoreronacional_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tesoreronacional_edit', { 'id': tesoreroNacional.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
